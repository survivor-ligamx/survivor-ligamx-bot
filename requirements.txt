# GENERADO AUTOMÁTICAMENTE DESDE pyproject.toml
# NO EDITES este archivo directamente.
# Fuente canónica: pyproject.toml
# Para regenerar: bash scripts/sync_deps.sh

# Dependencias del bot Survivor Liga MX (versiones fijadas para builds reproducibles).
# Probadas con Python 3.12 (ver runtime.txt). Actualiza con cuidado y corre
# `pytest tests/` antes de subir.

# Web / API
fastapi==0.141.1
uvicorn[standard]==0.39.0
slowapi==0.1.10

# Datos / modelo
numpy==2.0.2
scipy==1.13.1
pandas==2.3.3
requests==2.32.5
feedparser==6.0.12

# Base de datos (Postgres en prod; SQLite en local no necesita extra)
psycopg2-binary==2.9.12

# Utilidades
python-dotenv==1.2.1
tzdata==2026.2  # zona horaria CDMX (zoneinfo) en entornos sin tzdata del sistema
