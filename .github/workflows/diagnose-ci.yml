name: Capture CI diagnostics
on:
  push:
    branches: [copilot/mejora-planificador-survivor]
    paths: [.github/workflows/diagnose-ci.yml]
permissions:
  contents: write
jobs:
  diagnose:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install -r requirements-dev.txt
      - run: |
          : > ci-diagnostics.txt
          echo '=== RUFF CHECK ===' >> ci-diagnostics.txt
          ruff check . >> ci-diagnostics.txt 2>&1 || true
          echo '=== RUFF FORMAT ===' >> ci-diagnostics.txt
          ruff format --check . >> ci-diagnostics.txt 2>&1 || true
          echo '=== MYPY ===' >> ci-diagnostics.txt
          mypy src/ --show-error-codes >> ci-diagnostics.txt 2>&1 || true
          echo '=== PYTEST ===' >> ci-diagnostics.txt
          python -m pytest tests/ -q >> ci-diagnostics.txt 2>&1 || true
          rm .github/workflows/diagnose-ci.yml
          git config user.name "Mini manual fixer"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          git commit -m "chore: record CI diagnostics"
          git push
